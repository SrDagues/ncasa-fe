import{HttpClient,provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{firstValueFrom}from'rxjs';import{afterEach,beforeEach,describe,expect,it}from'vitest';import{Money}from'../../domain';import{HttpExpensePlanGateway}from'./http-expense-plan.gateway';
describe('HttpExpensePlanGateway',()=>{let http:HttpTestingController,gateway:HttpExpensePlanGateway;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});http=TestBed.inject(HttpTestingController);gateway=new HttpExpensePlanGateway(TestBed.inject(HttpClient),'/api');});afterEach(()=>http.verify());it('serializes a monthly installment plan',()=>{gateway.create('h1',{template:{description:'Rent',amount:Money.fromDecimal('900.00','EUR'),payerMemberId:'m1',categoryId:null,split:{type:'EQUAL',memberIds:['m1','m2']}},frequency:'MONTHLY',startDate:'2026-09-01',zoneId:'Europe/Madrid',endCondition:{type:'AFTER_OCCURRENCES',totalOccurrences:12},reminderDaysBefore:1}).subscribe();const request=http.expectOne('/api/households/h1/expense-plans');expect(request.request.body.template.amount).toBe('900.00');expect(request.request.body.endCondition).toEqual({type:'AFTER_OCCURRENCES',totalOccurrences:12});request.flush(planResponse);});it('sends plan filters and pagination',()=>{firstValueFrom(gateway.list('h1',{status:'ACTIVE',frequency:'MONTHLY'},{page:0,size:20}));const request=http.expectOne(r=>r.url==='/api/households/h1/expense-plans');expect(request.request.params.get('frequency')).toBe('MONTHLY');request.flush({content:[],page:0,size:20,totalElements:0,totalPages:0});});});
const planResponse={id:'p1',householdId:'h1',createdByMemberId:'m1',template:{description:'Rent',amount:'900.00',currency:'EUR',payerMemberId:'m1',categoryId:null,splitType:'EQUAL',allocations:[{memberId:'m1',amount:null,percentage:null}]},frequency:'MONTHLY',startDate:'2026-09-01',zoneId:'Europe/Madrid',endCondition:'AFTER_OCCURRENCES',endDate:null,totalOccurrences:12,reminderDaysBefore:1,materializedOccurrences:0,nextOccurrence:'2026-09-01',nextOccurrenceDueAt:'2026-09-01T00:00:00Z',nextReminderAt:'2026-08-31T00:00:00Z',status:'ACTIVE',pauseReason:null,cancellationReason:null,createdAt:'2026-08-25T00:00:00Z',updatedAt:'2026-08-25T00:00:00Z',pausedAt:null,cancelledAt:null,completedAt:null,version:0};
