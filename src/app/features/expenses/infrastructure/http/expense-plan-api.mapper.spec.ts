import{describe,expect,it}from'vitest';import{ExpenseApplicationError}from'../../application/expense.errors';import{mapExpensePlan,mapExpensePlanForecast}from'./expense-plan-api.mapper';
const plan={id:'p1',householdId:'h1',createdByMemberId:'m1',template:{description:'Rent',amount:'900.00',currency:'EUR',payerMemberId:'m1',categoryId:null,splitType:'EQUAL',allocations:[{memberId:'m1',amount:null,percentage:null}]},frequency:'MONTHLY',startDate:'2026-09-01',zoneId:'Europe/Madrid',endCondition:'AFTER_OCCURRENCES',endDate:null,totalOccurrences:12,reminderDaysBefore:1,materializedOccurrences:0,nextOccurrence:'2026-09-01',nextOccurrenceDueAt:'2026-08-31T22:00:00Z',nextReminderAt:'2026-08-30T22:00:00Z',status:'ACTIVE',pauseReason:null,cancellationReason:null,createdAt:'2026-08-25T10:00:00Z',updatedAt:'2026-08-25T10:00:00Z',pausedAt:null,cancelledAt:null,completedAt:null,version:0};
describe('expense plan mapper',()=>{it('maps an equal recurring plan without floating point money',()=>{const mapped=mapExpensePlan(plan);expect(mapped.template.amount.minorUnits).toBe(90000n);expect(mapped.endCondition).toEqual({type:'AFTER_OCCURRENCES',totalOccurrences:12});});it('rejects unknown lifecycle states',()=>expect(()=>mapExpensePlan({...plan,status:'BROKEN'})).toThrow(ExpenseApplicationError));it('maps forecast currencies independently',()=>{const value=mapExpensePlanForecast({householdId:'h1',from:'2026-09-01',to:'2026-12-01',currencies:[{currency:'EUR',total:'900.00'}],occurrences:[]});expect(value.currencies[0].total.toDecimal()).toBe('900.00');});});
