import { Component,computed,effect,ElementRef,inject,signal,viewChild } from '@angular/core';import { ActivatedRoute,RouterLink } from '@angular/router';import { TranslatePipe,TranslateService } from '@ngx-translate/core';import { LocalizedCurrencyPipe,LocalizedDatePipe } from '../../../../core/i18n/localized-format.pipe';import { ButtonComponent } from '../../../../shared/components/button/button.component';import { CardComponent } from '../../../../shared/components/card/card.component';import { EmptyStateComponent } from '../../../../shared/components/empty-state/empty-state.component';import { NotificationService } from '../../../../shared/components/notification/notification.service';import { HouseholdStore } from '../../../household';import { SettlementDetailStore } from './settlement.stores';
@Component({selector:'app-settlement-detail',imports:[RouterLink,TranslatePipe,LocalizedCurrencyPipe,LocalizedDatePipe,ButtonComponent,CardComponent,EmptyStateComponent],templateUrl:'./settlement-detail.component.html'})export class SettlementDetailComponent{protected readonly household=inject(HouseholdStore);protected readonly store=inject(SettlementDetailStore);private readonly route=inject(ActivatedRoute);private readonly notifications=inject(NotificationService);private readonly translate=inject(TranslateService);private readonly id=this.route.snapshot.paramMap.get('settlementId')??'';private readonly dialog=viewChild<ElementRef<HTMLDialogElement>>('dialog');private trigger:HTMLElement|null=null;protected readonly reason=signal('');protected readonly reasonError=signal(false);protected readonly permissionDenied=signal(false);protected readonly canVoid=computed(()=>{const s=this.store.settlement();const active=this.household.active();const summary=this.household.households().find(h=>h.id===active?.id);return !this.permissionDenied()&&s?.status==='CONFIRMED'&&(s.createdByMemberId===summary?.currentMemberId||summary?.currentRole==='ADMIN');});constructor(){effect(()=>{const household=this.household.active()?.id;if(household&&this.id)void this.store.load(household,this.id);});}protected memberName(id:string):string{return this.household.active()?.members.find(m=>m.id===id)?.email??`${id.slice(0,8)}…`;}protected retry():void{const household=this.household.active()?.id;if(household)void this.store.load(household,this.id);}protected open(event:Event):void{this.trigger=event.currentTarget as HTMLElement;this.reason.set('');this.reasonError.set(false);this.dialog()?.nativeElement.showModal();queueMicrotask(()=>this.dialog()?.nativeElement.querySelector('textarea')?.focus());}protected cancel(event?:Event):void{event?.preventDefault();if(this.store.pending())return;this.dialog()?.nativeElement.close();const trigger=this.trigger;this.trigger=null;queueMicrotask(()=>trigger?.focus());}protected async confirm():Promise<void>{const reason=this.reason().trim();this.reasonError.set(reason.length<1||reason.length>500);const household=this.household.active()?.id;if(this.reasonError()||!household)return;const ok=await this.store.void(household,this.id,reason);if(ok){this.cancel();this.notifications.show({id:'settlement-voided',tone:'positive',message:this.translate.instant('settlements.voidedMessage'),durationMs:5000});}else if(this.store.error()?.kind==='forbidden'){this.permissionDenied.set(true);this.cancel();}}}
