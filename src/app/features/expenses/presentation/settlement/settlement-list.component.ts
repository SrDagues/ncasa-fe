import { Component,DestroyRef,effect,inject,signal } from '@angular/core';import { takeUntilDestroyed } from '@angular/core/rxjs-interop';import { ActivatedRoute,Router,RouterLink } from '@angular/router';import { TranslatePipe } from '@ngx-translate/core';import { LocalizedCurrencyPipe,LocalizedDatePipe } from '../../../../core/i18n/localized-format.pipe';import { ButtonComponent } from '../../../../shared/components/button/button.component';import { CardComponent } from '../../../../shared/components/card/card.component';import { EmptyStateComponent } from '../../../../shared/components/empty-state/empty-state.component';import { HouseholdStore } from '../../../household';import { SettlementStatus } from '../../domain';import { SettlementListStore } from './settlement.stores';
@Component({selector:'app-settlement-list',imports:[RouterLink,TranslatePipe,LocalizedCurrencyPipe,LocalizedDatePipe,ButtonComponent,CardComponent,EmptyStateComponent],templateUrl:'./settlement-list.component.html'}) export class SettlementListComponent {protected readonly household=inject(HouseholdStore);protected readonly store=inject(SettlementListStore);private readonly route=inject(ActivatedRoute);private readonly router=inject(Router);private readonly destroyRef=inject(DestroyRef);protected readonly status=signal<SettlementStatus>('CONFIRMED');protected readonly from=signal('');protected readonly to=signal('');protected readonly memberId=signal('');protected readonly page=signal(0);protected readonly dateError=signal(false);constructor(){this.route.queryParamMap.pipe(takeUntilDestroyed(this.destroyRef)).subscribe(p=>{this.status.set(p.get('status')==='VOIDED'?'VOIDED':'CONFIRMED');this.from.set(p.get('from')??'');this.to.set(p.get('to')??'');this.memberId.set(p.get('memberId')??'');this.page.set(Math.max(0,Number(p.get('page'))||0));this.reload();});effect(()=>{this.household.active()?.id;this.reload();});}protected reload():void{const id=this.household.active()?.id;if(id)void this.store.load(id,{status:this.status(),from:this.from()||undefined,to:this.to()||undefined,memberId:this.memberId()||undefined},this.page());else this.store.reset();}protected apply():void{this.dateError.set(!!this.from()&&!!this.to()&&this.from()>this.to());if(!this.dateError())void this.navigate(0);}protected setStatus(status:SettlementStatus):void{this.status.set(status);void this.navigate(0);}protected previous():void{if(this.page()>0)void this.navigate(this.page()-1);}protected next():void{if((this.store.page()?.totalPages??0)>this.page()+1)void this.navigate(this.page()+1);}protected memberName(id:string):string{return this.household.active()?.members.find(m=>m.id===id)?.email??`${id.slice(0,8)}…`;}private navigate(page:number):Promise<boolean>{return this.router.navigate([],{relativeTo:this.route,queryParams:{status:this.status(),from:this.from()||null,to:this.to()||null,memberId:this.memberId()||null,page:page||null},queryParamsHandling:'merge'});}}
