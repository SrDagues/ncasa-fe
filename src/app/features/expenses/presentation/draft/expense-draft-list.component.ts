import { Component,effect,inject } from '@angular/core';import { RouterLink } from '@angular/router';import { TranslatePipe,TranslateService } from '@ngx-translate/core';import { LocalizedCurrencyPipe,LocalizedDatePipe } from '../../../../core/i18n/localized-format.pipe';import { ButtonComponent } from '../../../../shared/components/button/button.component';import { CardComponent } from '../../../../shared/components/card/card.component';import { EmptyStateComponent } from '../../../../shared/components/empty-state/empty-state.component';import { ConfirmDialogService } from '../../../../shared/components/confirm-dialog/confirm-dialog.service';import { HouseholdStore } from '../../../household';import { ExpenseDraft } from '../../domain';import { ExpensesSectionNavComponent } from '../navigation/expenses-section-nav.component';import { ExpenseDraftListStore } from './expense-draft-list.store';
@Component({selector:'app-expense-draft-list',imports:[RouterLink,TranslatePipe,LocalizedCurrencyPipe,LocalizedDatePipe,ButtonComponent,CardComponent,EmptyStateComponent,ExpensesSectionNavComponent],templateUrl:'./expense-draft-list.component.html'})export class ExpenseDraftListComponent{protected readonly household=inject(HouseholdStore);protected readonly store=inject(ExpenseDraftListStore);private readonly confirms=inject(ConfirmDialogService);private readonly translate=inject(TranslateService);constructor(){effect(()=>{const id=this.household.active()?.id;if(id)void this.store.load(id);else this.store.reset();});}protected async discard(draft:ExpenseDraft){const householdId=this.household.active()?.id;if(!householdId)return;if(await this.confirms.open({title:this.translate.instant('expenseDrafts.discardTitle'),message:this.translate.instant('expenseDrafts.discardMessage'),confirmLabel:this.translate.instant('expenseDrafts.discard'),cancelLabel:this.translate.instant('common.cancel'),variant:'destructive'}))await this.store.discard(householdId,draft);}}
